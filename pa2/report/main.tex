
\documentclass[conference]{IEEEtran}

\ifCLASSINFOpdf

\else

\fi

\hyphenation{op-tical net-works semi-conduc-tor}
\usepackage{amsmath}
\usepackage{caption}
\usepackage{float}
\usepackage{graphicx}
\usepackage{svg}
\svgpath{{images/}{./images/}}

\begin{document}

\title{WEIR - Assignment 2 - Document Retrieval}



\author{\IEEEauthorblockN{Maj Bregar, Gal Fajon, Alen Leban}

\IEEEauthorblockA{ Faculty of Computer and Information Science\\Večna pot 113, 1000 Ljubljana}
}

\maketitle

\begin{abstract}
This assignment builds on the PA1 crawl by extracting meaningful content from HTML pages, segmenting it into coherent text segments, encoding them with a sentence embedding model, and storing them in a pgvector-enabled PostgreSQL database for semantic retrieval.

We implement parsing tailored to 24ur.com articles, apply sentence-aware chunking, index embeddings with HNSW, and provide a query and reranking pipeline based on vector similarity and a cross-encoder reranker.
\end{abstract}

\IEEEpeerreviewmaketitle





\section{Introduction}
This report describes the design and implementation of a parsing and semantic embedding
pipeline that processes the HTML pages collected by the preferential web crawler developed
in the previous assignment. The goal is to extract clean article text from the crawled
pages and represent it in a form that supports efficient domain-specific information retrieval.

The pipeline consists of two main stages: preprocessing and retrieval. During preprocessing, crawled HTML pages from 24ur.com are parsed, and the extracted text is divided into sentence-based segments. The resulting segments are then embedded using a transformer-based sentence embedding model and stored in a vector database.

During retrieval, a user query is embedded using the same embedding model as for the segments and used to retrieve the most similar segments from the database. Query--segment pairs are subsequently evaluated by a reranking model, which refines the initial ranking based on semantic relevance. The final output of the pipeline is a ranked list of text segments that are most semantically relevant to the query.





\section{Methodology}


\subsection{Website filtering and data source}
We reuse the PA1 crawl database from the previous assignment and restrict processing to HTML pages with non-empty \texttt{html\_content}. In our pipeline this filtering is performed in a database query that fetches page IDs and HTML bodies.



\subsection{HTML parsing with XPath and regural expressions}
HTML parsing is implemented with \texttt{lxml}. We extract the article title by first reading the OpenGraph metadata (\texttt{//meta[@property='og:title']/@content}); if missing, we fall back to \texttt{//title}. A regex post-process removes the site suffix ``\texttt{| 24ur.com}'' and normalizes whitespace. The main article body is extracted from the \texttt{\#article-body} node using XPath, and non-content elements (\texttt{script}, \texttt{style}, \texttt{noscript}, \texttt{img}, \texttt{figure}, \texttt{svg}, \texttt{picture}) are removed to keep only the textual content.

We additionally capture the lead summary by locating the main article picture and the adjacent summary paragraph via XPath, then prepend it to the article body. A regex is used for lightweight text normalization (whitespace collapsing) and title cleanup.

This hybrid approach keeps extraction robust while remaining lightweight and transparent.








\subsection{Chunking}

After parsing the crawled web pages, the extracted text is divided into smaller sentence-based segments. Sentence boundaries are detected using the NLTK sentence tokenizer configured for the Slovene language. Segments are constructed incrementally by appending consecutive sentences until a predefined maximum word limit is reached. Once the limit is exceeded, a new segment is created. This approach preserves local semantic context while ensuring that segment sizes remain suitable for transformer-based embedding models.







\subsection{Segment Embedding}

Document segments were transformed into dense vector representations using transformer-based sentence embedding models. Each segment was embedded independently, and the resulting vectors were stored in dedicated database tables grouped by embedding dimensionality. We extend the PA1 database with a \texttt{cleaned\_content} column and three segment tables for different embedding sizes: \texttt{page\_segment\_vec384}, \texttt{page\_segment\_vec768}, and \texttt{page\_segment\_vec1024}. Each segment row stores the page ID, the model ID, the text segment, and the vector embedding. A separate \texttt{model} table records the embedding model name, enabling multiple models to coexist.


To ensure consistent similarity computation during retrieval, embeddings generated by all evaluated models were normalized to unit length prior to storage, despite the embedding models already producing nearly unit-length vectors.

The embedding computation included both multilingual and Slovenian-specialized embedding models. Table~\ref{tab:embedding_models} summarizes the embedding models used in the experiments together with their output vector dimensionalities.

\begin{table}[h]
\centering
\caption{Embedding models used for vectorization}
\label{tab:embedding_models}
\begin{tabular}{|l|c|}
\hline
\textbf{Model} & \textbf{Vec. Length} \\
\hline
\texttt{sentence-transformers/all-MiniLM-L6-v2} & 384 \\
\texttt{sentence-transformers/all-mpnet-base-v2} & 768 \\
\texttt{EMBEDDIA/sloberta} & 768 \\
\texttt{cjvt/crosloengual-bert-si-nli} & 768 \\
\texttt{BAAI/bge-m3} & 1024 \\
\hline
\end{tabular}
\end{table}

To support efficient similarity-based retrieval, vector indexes were created for each embedding table using the \texttt{pgvector} PostgreSQL extension. Separate HNSW indexes were generated for cosine similarity and Euclidean (L2) distance metrics, enabling experimentation with different retrieval strategies. Additionally, standard indexes were created for embedding model identifiers to enable efficient filtering by model. Since \texttt{pgvector} does not provide indexing support for L1 distance, L1 retrieval is performed without vector indexing.







\subsection{Segment Retrieval and Reranking}

During retrieval, the user query is embedded using the same embedding model as the stored segment embeddings. The generated query vector is then used to query the vector database and retrieve the top 100 most similar segments according to the selected distance metric. 

The retrieved segments are subsequently reranked using a cross-encoder model (also ''reranking'' model from now on). For reranking, query--segment text pairs are constructed and processed jointly by the reranker model to produce relevance scores. The final retrieval output consists of the 10 highest-ranked segments according to the reranker scores.

Table~\ref{tab:reranker_models} summarizes the reranking models used in the experiments.

\begin{table}[h]
\centering
\caption{Reranking model names}
\label{tab:reranker_models}
\begin{tabular}{|l|}
\hline
\textbf{Reranker Model} \\
\hline
\texttt{cross-encoder/ms-marco-MiniLM-L6-v2} \\
\texttt{cross-encoder/mmarco-mMiniLMv2-L12-H384-v1} \\
\texttt{BAAI/bge-reranker-v2-m3} \\
\hline
\end{tabular}
\end{table}







\section{Embedding Analysis}

To evaluate the retrieval quality of our embedding models, we employ the Normalized Discounted Cumulative Gain metric at \(k\) retrieved segments (NDCG@\(k\)). This metric is well suited for retrieval pipelines, as it measures not only the number of relevant segments among the top \(k\) retrieved results, but also whether the most relevant segments appear near the top of the ranking. For evaluation, we compute the mean NDCG@\(k\) across multiple queries, providing a more representative measure of average retrieval performance.








\subsection{Baseline Embedding Model Retrieval Performance}

First, we evaluated the quality of the embedding models by retrieving N closest segment embeddings to each of the query embeddings according to the cosine distance metric (other metrics were tested as well). This provides a baseline performance measure for selecting the best-performing embedding model without introducing bias from reranking models.

Queries used when computing mean NDCG@k scores were obtained by randomly sampling pages from the database and taking the first segment of each page. Only the first 128 characters of the chosen segments were used to extract a portion of the title. After we retrieved N closest segments to each query (for each model), ground truth relevancy score was calculated with different strategies: BM25 score, higher score for same page segments, and comparing to a reranked order using one of the reranking models. We present measurement for each strategy separately and don't combine them because the final performance measurements would be greatly influenced by how all three strategies were weighted. More about the reasons for choosing such strategies is told in the Section \ref{discussion}.

For the BM25 strategy, we used the ''Lucene'' version of the BM25 algorithm and computed scores for each retrieved result. The top 10 highest scoring results kept their original scores, while others were set to 0 to emphasize the difference between relevant and irrelevant results. Measurements using this strategy are shown in Figure \ref{fig:model_comparison_bm25}.

\begin{figure}
    \centering
    \includesvg[width=\columnwidth]{NDCG_models_bm25}
    \caption{Comparison of embedding models using the mean NDCG@\(k\) metric with the BM25 strategy.}
    \label{fig:model_comparison_bm25}
\end{figure}

The second strategy focused on giving higher scores to segments that were taken from the same page as the query since it makes sense that segments on the same page talk about the article story. Therefore segments on the same page as the query are given a score 1, otherwise 0. Measurements using this strategy are shown in Figure \ref{fig:model_comparison_same_page}.

\begin{figure}
    \centering
    \includesvg[width=\columnwidth]{NDCG_models_same_page}
    \caption{Comparison of embedding models using the mean NDCG@\(k\) metric with the same page strategy.}
    \label{fig:model_comparison_same_page}
\end{figure}

The third strategy simply compares orderings of each embedding model with their reranked orderings using the \texttt{ms-marco-MiniLM-L6-v2} model. An embedding model that produced an ordering closer to its reranked ordering is considered better. Measurements using this strategy are shown in Figure~\ref{fig:model_comparison_same_page}.

\begin{figure}
    \centering
    \includesvg[width=\columnwidth]{NDCG_models_reranked_optimal}
    \caption{Comparison of embedding models using the mean NDCG@\(k\) metric with the comparison of retrieved and reranked orderings strategy.}
    \label{fig:model_comparison_reranked_optimal}
\end{figure}


Our last evaluation strategy used an LLM to semantically assess the relevance of retrieved segments to a query. Each segment was assigned a relevance score from 0 (irrelevant) to 4 (highly relevant and directly related to the query). To emphasize highly relevant results in the ranking metric, the scores were transformed using an exponential function before computing NDCG@k.

For evaluation, \texttt{gpt-4.1-mini} was used to score all 50 retrieved segments for each query in a single pass. NDCG@k was then computed for values of \(k\) ranging from 2 to 50. Figure~\ref{fig:llm_scored_models} presents the mean retrieval performance of the evaluated embedding models using cosine similarity over 20 queries.

\begin{figure}[ht]
    \centering
    \includesvg[width=\linewidth]{images/all_models_llm_scored}
    \caption{LLM-based NDCG@k evaluation of embedding models using cosine similarity.}
    \label{fig:llm_scored_models}
\end{figure}











\subsection{Distance Metric Performance}

We evaluated the impact of different distance metrics for measuring embedding similarity. Cosine, L1, and L2 distances were compared using the \texttt{bge-m3} embedding model. The evaluation followed the same LLM-based scoring procedure described previously, with NDCG@k averaged over 50 queries.

\begin{figure}[ht]
    \centering
    \includesvg[width=\linewidth]{images/metrics}
    \caption{Comparison of distance metrics using LLM-based NDCG@k evaluation.}
    \label{fig:metric_comparison}
\end{figure}






\subsection{Reranker Performance}

Finally, we evaluated the performance of all embedding model and reranker combinations by computing the mean NDCG@k over 20 queries. Ground-truth relevance scores were obtained using the same LLM-based evaluation procedure described previously. For each query, the top 200 segments were first retrieved using cosine similarity on the embedding vectors, after which the reranker models reordered the results and returned the top 50 segments.

\begin{figure}[ht]
    \centering
    \includesvg[width=\linewidth]{images/model_reranker_llm_scored}
    \caption{LLM-based NDCG@k evaluation of embedding model and reranker combinations.}
    \label{fig:reranker_comparison}
\end{figure}





\subsection{Embedding Visualization}


To evaluate how effectively the models group embedded segments, we visualized embeddings using dimensionality reduction techniques. The original embeddings are simply projected onto a two-dimensional space using UMAP (PCA and tSNE were tried, but UMAP showed best clustering). Each point in the scatter plot represents a single segment embedding. Points of the same colors represent results of the same query, where the query point is marked as ''X''. In almost all plots there was one more cluster with fewer segments far away from others, which was filtered out to retain sensible plot proportions. More about the excluded cluster is said in the Section \ref{discussion}. 

\begin{figure}
    \centering
    \includegraphics[width=0.75\columnwidth]{images/Clusters.png}
    \caption{UMAP visualization of segment embedding clusters.}
    \label{fig:chunk_clustering}
\end{figure}




\section{Results}

The analysis results indicate that the model \texttt{bge-m3}~\cite{bgem3} consistently outperforms the other evaluated models, although much less convincingly according to the BM25 strategy. Among all models, \texttt{sloberta} was the worst. Surprisingly, \texttt{all-MiniLM} was as good or better that all 768-dimensional embedding models while having twice less embedding dimensions.

Figure~\ref{fig:metric_comparison} shows the relationship between the evaluated metrics. Since all embeddings were normalized, cosine similarity and L2 distance produced identical retrieval rankings and therefore nearly identical NDCG@k scores. In contrast, the L1 distance consistently underperformed, likely due to the curse of dimensionality in high-dimensional embedding spaces and the lost magnitude in embeddings due to normalization. Based on these results, cosine similarity was selected for the final retrieval pipeline.

Figure~\ref{fig:reranker_comparison} presents the performance of all evaluated combinations. The \texttt{mmarco-mMiniLMv2-L12-H384-v1} \cite{mmarco} reranker consistently provided the largest performance improvements across embedding models. In contrast, \texttt{bge-reranker-v2-m3} achieved lower performance despite being the largest evaluated reranker model. We attribute this to weaker Slovene language representation during training, despite the model being advertised as multilingual.

The best overall performance was achieved by combining the \texttt{bge-m3} embedding model with the \texttt{mmarco-mMiniLMv2-L12-H384-v1} cross-encoder reranker. Due to their consistently strong retrieval performance, this combination was selected for the final retrieval pipeline.

Additionally, we observed that the NDCG@k scores plateaued for nearly all evaluated models after \(k=10\). This suggests that retrieving additional segments beyond the top 10 provides little improvement in retrieval quality. Based on this observation, we selected 10 as the final number of retrieved segments in the final pipeline.


The final selected parameters of our pipeline are noted in Table~\ref{tab:final_params}. 

\begin{table}[h]
\centering
\caption{Final pipeline parameters}
\label{tab:final_params}
\begin{tabular}{|l|c|}
\hline
\textbf{Parameter} & \textbf{Chosen Value} \\
\hline
\texttt{Embedding Model} & BAAI/bge-m3 \\
\texttt{Reranking Model} & mmarco-mMiniLMv2-L12-H384-v1 \\
\texttt{Segment Max Length} & 128 words \\
\texttt{Retrieval Dist. Metric} & cosine \\ 
\texttt{Similarity Return n} & 100 \\
\texttt{Reranking Return k} & 10 \\
\hline
\end{tabular}
\end{table}

Unsurprisingly, query results of the \texttt{bge-m3} model are also better grouped in the projected embedding plots than other models. The worst grouping comes from the \texttt{sloberta} model, where many queries are always put on the same island, possibly suggesting the model is having a harder time separating them.



\section{Disscussion} \label{discussion}

The parser is tailored to 24ur.com and relies on stable HTML structure. Pages that deviate from the \texttt{\#article-body} template or heavily script-rendered content can yield empty or partial text. Chunking relies on sentence tokenization quality and may produce uneven segment sizes. Reranking improves accuracy but adds latency, which may be problematic for interactive settings.

For measuring retrieval quality of embedding and reranking models we would ideally have a manually annotated dataset of queries, their results, and relevance scores. Since we couldn't afford to make one, we opt to employ diferent algorithms and strategies to calculate ground truth relevance scores and measured models on each one. We think that the least reliable strategy was the BM25 one since embedding models are expected to encode meanings of the tet other that just simple term frequencies. 

For the NDCG@k metric we were expecting to see the plot starting high (with many relevant results in the first positions), then dipping to the lowest point at around k=10 (less relevant results in the top ranks) and the slowly growing back up (gathering the rest of the relevant results). The original 3 strategies don't have a pronounced high start, but the strategy using the \texttt{gpt-4.1-mini} model did. Because it was discovered towards the end of experimentation, we used it on the last few plots only.

Experiments however clearly show reranking significantly improves the relevancy of top results, but due to the harder interpretability of the NDCG@k metric concrete relative improvements between models are harder to determine.

UMAP visualizations helped us somewhat validate our measurements and we did keep in mind that the visualization inaccurately represents the real embedding space. We hoped however that if a model was able to extract many useful features and group segments around them, we would see more groups in the UMAP visualization as well. For each model there is also a hidden cluster of segments that wasn't plotted to prevent unusual plot proportions, but that always seems to be a cluster of article keywords, possibly because they don't contain enough information on their own to be put among longer segments. This becomes problematic when using short, keyword-like queries, which get embedded into that cluster, causing the retrieval to contain mostly keyword results that were found near the query. 

In the Appendix you can find an example of a short query, the retrieved results and how different rerankers also treat such results (Tables~\ref{tab:short_query}, \ref{tab:short_query_reranked_low_end}, \ref{tab:short_query_reranked_high_end}). 

Other examples include a longer query that was taken as a title from a random 24ur.com article. In tables \ref{tab:long_query} and \ref{tab:long_query_reranked} are retrieved and reranked results, where it is noticable that reranking put into the top ranks more relevant results that the retriever alone. The reranker successfully found the original article from where the query was taken.

Finally, an off-topic query (about opening times of a zoo in Slovenia) was used to see how both an embedding and a reranking model deal with such queries (Tables~\ref{tab:unusual_query}, \ref{tab:unusual_query_reranked}). Reranked results have much lower cross scores and larger distances, which is as expected. While the top retrieved segment is the most relevant (a keyword ''zoo''), it surprisingly doesn't appear in the reranked results. Other results also don't seem too relevant to us authors, so not even the reranker could improve this situation. 

% - what can be done better, what we could expand on
% - TODO EXPAND LATER

\section{Conclusion}
We implemented an end-to-end extraction and retrieval pipeline for the PA1 crawl of 24ur.com. The system parses raw HTML, chunks text using sentence boundaries, stores embeddings in a pgvector database with HNSW indexing, and retrieves and reranks results for user queries. The approach is modular, supports multiple embedding models, and can be easily configured using an \texttt{.env} file.


\bibliographystyle{IEEEtran}
\bibliography{ref}

\onecolumn
\appendix

\begin{table}[h]
\centering
\small
\begin{tabular}{|c|p{13cm}|c|}
    \hline
    \textbf{Rank} & \textbf{Retrieved Text} & \textbf{Distance} \\
\hline
1 & Vladimir putin. & 0.0432 \\
\hline
2 & vladimir putin. & 0.0432 \\
\hline
3 & Vladimir Putin. & 0.0432 \\
\hline
4 & putin. & 0.1489 \\
\hline
5 & Putin. & 0.1489 \\
\hline
6 & vladimi putin. & 0.1500 \\
\hline
7 & vladmir putin. & 0.1539 \\
\hline
% 8 & Vladimir. & 0.2432 \\
% \hline
% 9 & vladimir. & 0.2432 \\
% \hline
% 10 & Litvinenko. & 0.3473 \\
% \hline
\end{tabular}
\caption{Top 7 retrieval results using the \texttt{all-MiniLM} model before reranking for query=''Vladimir Putin''.}
\label{tab:short_query}
\end{table}


\begin{table}[h]
\centering
\small
\begin{tabular}{|c|p{13cm}|c|c|}
\hline
\textbf{Rank} & \textbf{Reranked Text} & \textbf{Cross Score} & \textbf{Distance} \\
\hline
1 & Vladimir putin. & 7.190 & 0.0432 \\
\hline
2 & vladimir putin. & 7.190 & 0.0432 \\
\hline
3 & Vladimir Putin. & 7.190 & 0.0432 \\
\hline
4 & Novoizvoljen ruski predsednik. Vladimir Putin je po osmih letih vladanja krmilo države predal 13 let mlajšemu političnemu varovancu Dmitirju Medvedjevu. Ruski p... & 6.703 & 0.4193 \\
\hline
5 & Putin je optimističen: Najhuje je mimo. Ruski predsednik Vladimir Putin je na tradicionalnem televizijskem maratonu odgovarjal na vprašanja državljanov. Vprašan... & 6.672 & 0.4288 \\
\hline
6 & Ruski predsednik Vladimir Putin in njegov ukrajinski kolega Volodimir Zelenski sta sicer med prvim telefonskim pogovorom v začetku meseca govorila o možnosti za... & 6.504 & 0.4321 \\
\hline
7 & 1 7 Vladimir Putin in venezuelski predsednik Nicolas Maduro APVladimir Putin in kitajski predsednik Ši Džinping ProfimediaVladimir Putin in brazilski predsednik... & 6.268 & 0.4156 \\
\hline
% 8 & Putin: Nobena država nima česa takšnega, raketa je neranljiva za zračno obrambo. ZDA: Smo povsem pripravljeni. Vladimir Putin je v letnem nagovoru poslancem spo... & 6.061 & 0.4377 \\
% \hline
% 9 & vladimi putin. & 5.999 & 0.1500 \\
% \hline
% 10 & Zakaj je Putin med obiskom v Moskvi Stevu Witkoffu predal red Lenina?. Ruski predsednik Vladimir Putin je med obiskom v Moskvi ameriškemu posebnemu odposlancu p... & 5.731 & 0.4451 \\
% \hline
\end{tabular}
\caption{Top 7 retrieval results using the \texttt{all-MiniLM} model after reranking with the \texttt{ms-marco-MiniLM} model for query=''Vladimir Putin''.}
\label{tab:short_query_reranked_low_end}
\end{table}

\begin{table}[h]
\centering
\small
\begin{tabular}{|c|p{13cm}|c|c|}
\hline
\textbf{Rank} & \textbf{Reranked Text} & \textbf{Cross Score} & \textbf{Distance} \\
\hline
1 & Vladimir Putin. & 1.0000 & 0.0432 \\
\hline
2 & Vladimir putin. & 0.9999 & 0.0432 \\
\hline
3 & vladimir putin. & 0.9999 & 0.0432 \\
\hline
4 & vladmir putin. & 0.9999 & 0.1539 \\
\hline
5 & vladimi putin. & 0.9998 & 0.1500 \\
\hline
6 & Putin. & 0.9996 & 0.1489 \\
\hline
7 & Vladimir. & 0.9987 & 0.2432 \\
% \hline
% 8 & vladimir. & 0.9984 & 0.2432 \\
% \hline
% 9 & Vladislav. & 0.9982 & 0.4304 \\
% \hline
% 10 & vladimir pustin. & 0.9979 & 0.3915 \\
\hline
\end{tabular}
\caption{Top 7 retrieval results using the \texttt{all-MiniLM} model after reranking with the better \texttt{bge-reranker} model for query=''Vladimir Putin''.}
\label{tab:short_query_reranked_high_end}
\end{table}

\begin{table}
\centering
\small
\begin{tabular}{|c|p{13cm}|c|}
\hline
\textbf{Rank} & \textbf{Retrieved Text} & \textbf{Distance} \\
\hline
1 & Rusi demonstrirali 'povratni jedrski napad': uporabili balistične rakete. Ruski predsednik Vladimir Putin je prek digitalne platforme spremljal vojaške vaje rus... & 0.2464 \\
\hline
2 & Prigožinov upor je predstavljal enega največjih izzivov za dolgoletno vladavino Vladimirja Putina. Vodja Wagnerjeve skupine je imel pomembno vlogo pri invaziji ... & 0.2496 \\
\hline
3 & Pele javno poziva Putina k zaustavitvi vojne. Brazilska nogometna legenda Pele je ruskega predsednika Vladimirja Putina javno pozval, naj konča vojno v Ukrajini... & 0.2525 \\
\hline
4 & Putin se zaradi oslabljene imunosti verjetno boji okužbe, so špekulirali. Pred kratkim so se pojavile tudi govorice, da Putina nenehno spremlja zdravnik, ki je ... & 0.2540 \\
\hline
5 & Zdi se, da Američani niso pripravljeni iti predaleč proti Rusiji, Evropejci pa še naprej podpirajo Ukrajino, zato smo v zastoju. "Kakšna je evropska pogajalska ... & 0.2602 \\
\hline
6 & Rusija je obtožbe zavrnila in poudarila, da je odločitev Ovseja znak naraščajočih napetosti med Rusijo in Zahodom. Putin je tako izjavil, da za bojkotom zahodni... & 0.2625 \\
\hline
7 & EU je v obrazložitvi, zakaj je 69-letni milijarder pristal na seznamu sankcioniranih, navedla, da je Usmanov "prokremeljski oligarh s posebno tesnimi povezavami... & 0.2636 \\
% \hline
% 8 & "Putin tukaj ustvarja določeno vzdušje, katerega cilj je spodkopavanje zaupanja javnosti v vlado, v ključne strukture, kot so oborožene sile in policija, ter us... & 0.2642 \\
% \hline
% 9 & Ukrajinska vlada je vztrajala pri prisotnosti poveljnika upornikov v Donecku Aleksandra Zaharčenka in voditelja separatistov v Lugansku Igorja Plonickija. O dana... & 0.2643 \\
% \hline
% 10 & Zelenski Rusom namenil sporočilo: Putin vas uničuje. Iz več ukrajinskih regij poročajo o raketnih napadih. V Kijevu je odjeknilo deset eksplozij. Za približno p... & 0.2647 \\
\hline
\end{tabular}
\caption{Top 7 retrieval results using the \texttt{all-MiniLM} model before reranking for query=''Zelenski Zahod poziva k odzivu, Putin: Rakete so pripravljene za uporabo.''.}
\label{tab:long_query}
\end{table}


% \begin{table}[ht]
% \centering
% \small
% \begin{tabular}{|c|p{13cm}|c|c|}
% \hline
% \textbf{Rank} & \textbf{Reranked Text} & \textbf{Cross Score} & \textbf{Distance} \\
% \hline
% 1 & Zelenski Zahod poziva k odzivu, Putin: Rakete so pripravljene za uporabo. Ruski predsednik Vladimir Putin je sporočil, da bo nadaljeval s testiranjem balistični... & 9.227 & 0.2823 \\
% \hline
% 2 & Zelenski Rusom namenil sporočilo: Putin vas uničuje. Iz več ukrajinskih regij poročajo o raketnih napadih. V Kijevu je odjeknilo deset eksplozij. Za približno p... & 6.814 & 0.2647 \\
% \hline
% 3 & Rusi demonstrirali 'povratni jedrski napad': uporabili balistične rakete. Ruski predsednik Vladimir Putin je prek digitalne platforme spremljal vojaške vaje rus... & 6.441 & 0.2464 \\
% \hline
% 4 & Dodal je, da so se z ZDA pripravljeni srečati na ravni voditeljev držav, da bi obravnavali občutljiva vprašanja. "O zadevah, kot so ozemeljska vprašanja, se je ... & 5.991 & 0.2833 \\
% \hline
% 5 & Na 1000. dan vojne v Ukrajini Putin podpisal odlok za širšo rabo jedrskega orožja. Ruski predsednik Vladimir Putin je podpisal odlok, ki Moskvi omogoča uporabo ... & 5.791 & 0.2684 \\
% \hline
% 6 & "Predsednik Putin je v nagovoru narodu dejal, da je regionalni konflikt v Ukrajini, kot ga je poimenoval, po napadih na rusko regijo Brjansk in Kursk z ameriški... & 5.408 & 0.2744 \\
% \hline
% 7 & "Ne vidimo nobenih znakov, da se Rusija pripravlja na uporabo jedrskega orožja," je po Putinovih pripombah dejal ameriški državni sekretar Antony Blinken. Za ogl... & 5.368 & 0.2784 \\
% \hline
% 8 & Prav tako naj bi šlo šele za prvi val raketnih napadov. Guverner ukrajinske regije Mikolajev Vitalij Kim je že predhodno opozoril na možnost raketnih napadov. Uk... & 5.177 & 0.2764 \\
% \hline
% 9 & Prigožinov upor je predstavljal enega največjih izzivov za dolgoletno vladavino Vladimirja Putina. Vodja Wagnerjeve skupine je imel pomembno vlogo pri invaziji ... & 5.081 & 0.2496 \\
% \hline
% 10 & Generalni sekretar Nata Jens Stoltenberg je medtem zaradi Putinove odločitve izrazil zaskrbljenost. Sile za odvračanje so enote, katerih namen je odvrniti napad ... & 4.513 & 0.2818 \\
% \hline
% \end{tabular}
% \caption{Top 10 retrieval results using the \texttt{all-MiniLM} model after reranking with the \texttt{ms-marco-MiniLM} model for query=''Zelenski Zahod poziva k odzivu, Putin: Rakete so pripravljene za uporabo.''.}
% \label{tab:reranked_results_2}
% \end{table}

\begin{table}
\centering
\small
\begin{tabular}{|c|p{13cm}|c|c|}
\hline
\textbf{Rank} & \textbf{Reranked Text} & \textbf{Cross Score} & \textbf{Distance} \\
\hline
1 & Zelenski Zahod poziva k odzivu, Putin: Rakete so pripravljene za uporabo. Ruski predsednik Vladimir Putin je sporočil, da bo nadaljeval s testiranjem balistični... & 0.9993 & 0.2823 \\
\hline
2 & Prav tako naj bi šlo šele za prvi val raketnih napadov. Guverner ukrajinske regije Mikolajev Vitalij Kim je že predhodno opozoril na možnost raketnih napadov. Uk... & 0.9129 & 0.2764 \\
\hline
3 & Dodal je, da so se z ZDA pripravljeni srečati na ravni voditeljev držav, da bi obravnavali občutljiva vprašanja. "O zadevah, kot so ozemeljska vprašanja, se je ... & 0.6005 & 0.2833 \\
\hline
4 & Rusi demonstrirali 'povratni jedrski napad': uporabili balistične rakete. Ruski predsednik Vladimir Putin je prek digitalne platforme spremljal vojaške vaje rus... & 0.5256 & 0.2464 \\
\hline
5 & Na 1000. dan vojne v Ukrajini Putin podpisal odlok za širšo rabo jedrskega orožja. Ruski predsednik Vladimir Putin je podpisal odlok, ki Moskvi omogoča uporabo ... & 0.4899 & 0.2684 \\
\hline
6 & "Predsednik Putin je v nagovoru narodu dejal, da je regionalni konflikt v Ukrajini, kot ga je poimenoval, po napadih na rusko regijo Brjansk in Kursk z ameriški... & 0.3710 & 0.2744 \\
\hline
7 & Zelenski Rusom namenil sporočilo: Putin vas uničuje. Iz več ukrajinskih regij poročajo o raketnih napadih. V Kijevu je odjeknilo deset eksplozij. Za približno p... & 0.2555 & 0.2647 \\
% \hline
% 8 & Ruski predsednik Vladimir Putin je v ponedeljek obljubil "primeren odgovor" na napad, katerega motivacijo je predstavil kot pretežno politično. "Naša glavna nal... & 0.2171 & 0.2744 \\
% \hline
% 9 & Putin: Rusija bo jedrsko orožje uporabila le, če bo napadena. Ruski predsednik Vladimir Putin je v nagovoru na letnem zasedanju kremeljskega sveta za človekove ... & 0.1816 & 0.2832 \\
% \hline
% 10 & Generalni sekretar Nata Jens Stoltenberg je medtem zaradi Putinove odločitve izrazil zaskrbljenost. Sile za odvračanje so enote, katerih namen je odvrniti napad ... & 0.1690 & 0.2818 \\
\hline
\end{tabular}
\caption{Top 7 retrieval results using the \texttt{all-MiniLM} model after reranking with the \texttt{bge-reranker} model for query=''Zelenski Zahod poziva k odzivu, Putin: Rakete so pripravljene za uporabo.'' (model \texttt{all-MiniLM} was used here to demonstrate the difference between retrieved and reranked results more clearly).}
\label{tab:long_query_reranked}
\end{table}

\begin{table}
\centering
\small
\begin{tabular}{|c|p{12cm}|c|}
\hline
\textbf{Rank} & \textbf{Retrieved Text} & \textbf{Distance} \\
\hline
1 & živalski vrt. & 0.3488 \\
\hline
2 & Jevgenij Ziničev. & 0.3836 \\
\hline
3 & Več držav je že napovedalo njihovo dobavo. & 0.4197 \\
\hline
4 & Napovedal je, da bo danes govoril na zasedanju Varnostnega sveta ZN o Ukrajini. & 0.4291 \\
\hline
5 & Gre za območje, na katerem živijo predvsem armenski kristjani, a leži globoko na območju Azerbajdžana, katerega prebivalstvo je večinoma muslimansko. & 0.4344 \\
\hline
\end{tabular}
\caption{Top 10 retrieval results using the \texttt{all-MiniLM} model before reranking for an unusual query=''Ob katerih dneh je živalski vrt Zoo odprt.''.}
\label{tab:unusual_query}
\end{table}


\begin{table}
\centering
\small
\begin{tabular}{|c|p{12cm}|c|c|}
\hline
\textbf{Rank} & \textbf{Reranked Text} & \textbf{Cross Score} & \textbf{Distance} \\
\hline
1 & Voditelja sta se na kratko srečala le ob junijski slovesnosti ob dnevu D v Normandiji. & -2.582 & 0.4708 \\
\hline
2 & Dogovor naj bi vodil v končanje konflikta, v katerem je od aprila umrlo že skoraj 2900 ljudi. & -3.745 & 0.4594 \\
\hline
3 & Kljub temu je dogodek s prvega dne vojne postal del sodobne ukrajinske folklore, njihova pošta pa je celo izdala znamke, ki ilustrirajo famozni dogodek. & -4.047 & 0.4423 \\
\hline
4 & Obema so odpravili pripor, sodba je pravnomočna, je sporočilo okrožno sodišče v Ljubljani. & -4.690 & 0.4826 \\
\hline
5 & obletnica invazije. & -4.769 & 0.4658 \\
\hline
\end{tabular}
\caption{Top 10 retrieval results using the \texttt{all-MiniLM} model before reranking with the \texttt{mmarco-mMiniLMv2} model for an unusual query=''Ob katerih dneh je živalski vrt Zoo odprt.''.}
\label{tab:unusual_query_reranked}
\end{table}

\end{document}